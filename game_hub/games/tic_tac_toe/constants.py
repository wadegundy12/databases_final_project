from games.connect_four.constants import (
    BLACK,
    BLUE,
    RED,
    YELLOW,
    FONT,
    SMALL_FONT,
    width,
    height,
)

GRID_SIZE = 3
CELL_SIZE = 150
GRID_PIXELS = CELL_SIZE * GRID_SIZE
OFFSET_X = (width - GRID_PIXELS) // 2
OFFSET_Y = (height - GRID_PIXELS) // 2
